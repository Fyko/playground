// import type { APIInteraction } from 'discord-api-types/v10';
// import {
// 	isGuild,
// 	isGuildAutocomplete,
// 	isGuildChatInput,
// 	isGuildContextMenu,
// 	isGuildModalSubmit,
// 	isPing,
// } from './guards.js';

// declare const interaction: APIInteraction;

// (() => {
// 	if (isPing(interaction)) {
// 		interaction;
// 		//	^?
// 	}

// 	if (isGuild(interaction)) {
// 		interaction;
// 		//	^?

// 		if (isGuildChatInput(interaction)) {
// 			interaction;
// 			//	^?
// 		}

// 		if (isGuildAutocomplete(interaction)) {
// 			interaction;
// 			//	^?
// 		}

// 		if (isGuildModalSubmit(interaction)) {
// 			interaction;
// 			//	^?
// 		}

// 		if (isGuildContextMenu(interaction)) {
// 			interaction;
// 			//  ^?
// 		}
// 	}
// })();
export const x = 'git keep lol';
